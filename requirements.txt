# Phase 1 — synthetic data generation + data-quality tests.
# Later phases append their own deps (simpy, python-pptx, plotly).
# See FLAGSHIP_SPEC.md Section 3 for the full stack.
numpy>=1.26
pandas>=2.0
matplotlib>=3.7
pytest>=7.4

# Phase 2 — forecasting (Prophet primary, statsmodels SARIMA fallback).
prophet>=1.1
statsmodels>=0.14
